{$IFDEF ReadInterface}
// AStructuredTypeIdentifier ***************************************************
{ This class represents a symbol which identifies a new structured type.  A
  structured type may inherit from an existing type and adds additional items.
}
type AStructuredTypeIdentifier = class(ASimpleTypeIdentifier)
  protected
    { Refers to the  }
{$ENDIF}

